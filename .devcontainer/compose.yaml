---
name: stimulus-form
services:
  app:
    build:
      context: ".."
      dockerfile: ".devcontainer/Dockerfile"
    volumes:
      - "../..:/workspaces:cached"
    command: sleep infinity
    networks:
      - default
    ports:
      - 3030:3030
